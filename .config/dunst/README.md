A Pywal template (.config/wal/templates/dunstrc) is used to generate a dunstrc file with Pywal colours.
The exported file (.cache/wal/dunstc) is then symlinked to .config/dunst/dunstrc when X is started.
